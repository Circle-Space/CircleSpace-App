import { useImperativeHandle } from 'react';
export const useZoomableHandle = (ref, reset, zoom) => {
  useImperativeHandle(ref, () => ({
    reset() {
      reset();
    },
    zoom(event) {
      zoom(event);
    }
  }), [reset, zoom]);
};
//# sourceMappingURL=useZoomableHandle.js.map